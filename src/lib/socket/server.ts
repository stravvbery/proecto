// Socket.IO server logic has been moved to server.ts at project root.
// This file is kept for import compatibility but is no longer the entry point.
// The standalone socket server runs on port 3001 alongside Next.js on port 3000.

export {};
